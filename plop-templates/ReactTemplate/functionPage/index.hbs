import React from 'react';
import Page from '@/components/Page';

export default (props) => {
  console.log(props);
  return (<Page title="{{pageTitle}}" showGoBackBtn={false}>
    这是内容区域
  </Page>);
};
